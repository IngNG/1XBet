#include "TXLib.h"

int main()
    {
    txCreateWindow (1000, 800);
    txSetColor(TX_RED);
    txRectangle(100, 100, 400, 300);
    txRectangle(100, 100, 400, 200);

    return 0;
    }

